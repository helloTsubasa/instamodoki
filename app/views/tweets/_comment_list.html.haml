- tweet.comments.each do |comment|
  .comment-form
    = link_to comment.user.name, user_path(comment.user), class: "no-text-decoration black-color"
    %span= comment.comment
    - if comment.user == current_user
      = link_to tweet_comment_path(tweet.id, comment), method: :delete, remote: true, class: "delete-comment" do
        %i.fas.fa-times.btn-icon